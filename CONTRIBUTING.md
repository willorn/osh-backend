# Contributing

## 开发前

1. 从最新 `qa/**` 或负责人指定分支创建 feature/codex 分支。
2. 判断是否需要 OpenSpec change。
3. 阅读 `docs/ai/01-newcomer-guide.md` 和 `docs/ai/02-workflow.md`。

## 必须建 OpenSpec 的情况

- 新功能。
- 行为变化。
- 接口、权限、数据、中间件、部署变化。
- 基础组件升级。
- 微服务拆分。

## PR 前

- 确认 `openspec/changes/<change-id>` 已补齐。
- 运行相关构建或测试。
- 填写 PR 模板。
- 不提交密钥、token、日志、构建产物。

## 代码风格

见 `docs/ai/05-code-style.md` 和 `docs/ai/conventions/`。
