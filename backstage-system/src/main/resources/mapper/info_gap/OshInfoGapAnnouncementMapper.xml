<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.backstage.system.mapper.info_gap.OshInfoGapAnnouncementMapper">

    <select id="selectLatestInfoGapAnnouncementsByChannel"
            resultType="com.backstage.system.domain.dto.info_gap.InfoGapAnnoRespDTO">
        select id,
               title,
               link,
               create_time as createTime
        from osh_announcement
        where delete_flag = 0
          and status = 0
          and resource_type = 'info_gap'
          and channel = #{channel}
        order by create_time desc
        limit 5
    </select>

    <insert id="insertInfoGapAnnouncement">
        insert into osh_announcement (
            title,
            link,
            resource_type,
            channel,
            status,
            source,
            source_module,
            delete_flag,
            create_by,
            create_time,
            update_by,
            update_time
        ) values (
            #{title},
            #{link},
            'info_gap',
            #{channel},
            0,
            'system',
            'info_gap',
            0,
            #{operator},
            now(),
            #{operator},
            now()
        )
    </insert>

</mapper>
