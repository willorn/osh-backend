<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.backstage.system.mapper.tool.OshToolPackageMapper">

    <resultMap id="OshToolPackageResult" type="com.backstage.system.domain.tool.OshToolPackage">
        <id property="id" column="id"/>
        <result property="packageName" column="package_name"/>
        <result property="useCount" column="use_count"/>
        <result property="price" column="price"/>
        <result property="pointCost" column="point_cost"/>
        <result property="payType" column="pay_type"/>
        <result property="status" column="status"/>
        <result property="sortOrder" column="sort_order"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="deleteFlag" column="delete_flag"/>
    </resultMap>

    <resultMap id="ToolQuotaPackageVOResult" type="com.backstage.system.domain.vo.tool.ToolQuotaPackageVO">
        <id property="packageId" column="id"/>
        <result property="packageName" column="package_name"/>
        <result property="useCount" column="use_count"/>
        <result property="cashAmount" column="price"/>
        <result property="pointAmount" column="point_cost"/>
        <result property="payType" column="pay_type"/>
        <result property="status" column="status"/>
        <result property="sortOrder" column="sort_order"/>
    </resultMap>

    <select id="selectPackageById" resultMap="OshToolPackageResult">
        select id, package_name, use_count, price, point_cost, pay_type, status, sort_order,
               create_by, create_time, update_by, update_time, delete_flag
        from osh_tool_package
        where id = #{id}
          and delete_flag = 0
        limit 1
    </select>

    <select id="selectEnabledPackages" resultMap="OshToolPackageResult">
        select id, package_name, use_count, price, point_cost, pay_type, status, sort_order,
               create_by, create_time, update_by, update_time, delete_flag
        from osh_tool_package
        where status = 1
          and delete_flag = 0
        order by sort_order asc, id asc
    </select>

    <select id="selectAllPackageVos" resultMap="ToolQuotaPackageVOResult">
        select id, package_name, use_count, price, point_cost, pay_type, status, sort_order
        from osh_tool_package
        where delete_flag = 0
        order by sort_order asc, id asc
    </select>

    <insert id="insertPackage" parameterType="com.backstage.system.domain.tool.OshToolPackage"
            useGeneratedKeys="true" keyProperty="id">
        insert into osh_tool_package (
            package_name, use_count, price, point_cost, pay_type, status, sort_order,
            create_by, create_time, update_by, update_time, delete_flag
        ) values (
            #{packageName}, #{useCount}, #{price}, #{pointCost}, #{payType}, #{status}, #{sortOrder},
            #{createBy}, now(), #{updateBy}, now(), 0
        )
    </insert>

    <update id="updatePackage" parameterType="com.backstage.system.domain.tool.OshToolPackage">
        update osh_tool_package
        <set>
            <if test="packageName != null">package_name = #{packageName},</if>
            <if test="useCount != null">use_count = #{useCount},</if>
            <if test="price != null">price = #{price},</if>
            <if test="pointCost != null">point_cost = #{pointCost},</if>
            <if test="payType != null">pay_type = #{payType},</if>
            <if test="status != null">status = #{status},</if>
            <if test="sortOrder != null">sort_order = #{sortOrder},</if>
            update_by = #{updateBy},
            update_time = now()
        </set>
        where id = #{id}
          and delete_flag = 0
    </update>

    <update id="softDeletePackage">
        update osh_tool_package
        set delete_flag = 1,
            update_by = #{operator},
            update_time = now()
        where id = #{id}
          and delete_flag = 0
    </update>
</mapper>
