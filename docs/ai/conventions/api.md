# API 约定

## 路径

- 当前主网站接口以 `/pc/**` 为重要兼容面。
- Nuxt 前端开发环境会把 `/api/**` 代理到后端 `/pc/**`。
- 新接口不要随意绕开已有代理规则。

## 请求

- 参数命名清晰。
- 分页参数统一。
- ID 类型保持一致。
- 文件上传大小、类型、存储位置必须写明。

## 响应

- 保持统一响应包装。
- 不随意改变字段含义。
- 兼容旧字段时必须说明废弃策略。

## 错误码

- 权限失败、参数错误、资源不存在、业务冲突要区分。
- 错误消息对用户可理解，对开发可排查。
